/**
 * Created by lenovo on 28.06.2017.
 */
$(function(){
    var $day    = $('.rover-s-vm__date-day'),
        $month  = $('.rover-s-vm__date-month');

   /* if ($day.length)
        $day.fitText(.11);

    if ($month.length)
        $month.fitText(.35);*/
});